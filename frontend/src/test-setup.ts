// Vitest setup file. Currently only wires @testing-library/jest-dom so
// every test file can use the extended `expect` matchers (toBeInTheDocument
// etc.) without an import.
import "@testing-library/jest-dom/vitest";
